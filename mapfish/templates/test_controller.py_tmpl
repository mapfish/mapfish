from {{basePkg}}.tests import *

class Test{{contrClass}}Controller(TestController):
    def test_index(self):
        response = self.app.get(url(controller='{{fullModName}}'))
        # Test response...
